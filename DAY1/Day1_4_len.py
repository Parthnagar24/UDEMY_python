#The len() function in Python is used to get (not store) the length of an object, typically strings, lists, tuples, dictionaries, etc.

input=input("What is your name")
print(len(input))

length=len(input)
print(length)